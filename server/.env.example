# Cloudflare Workers AI (rotate token if ever exposed)
CF_ACCOUNT_ID=
CF_API_TOKEN=
CF_AI_MODEL=@cf/zai-org/glm-4.7-flash
# CF_AI_BASE_URL=https://api.cloudflare.com/client/v4/accounts/{ACCOUNT_ID}/ai/v1
# Upstream model wait seconds (default 180)
# CF_AI_TIMEOUT=180
# Chart reading max output tokens (default 5200)
# CF_CHART_AI_MAX_TOKENS=5200

LISTEN_HOST=127.0.0.1
PORT=8787
# Comma-separated origins, or * for dev
ALLOWED_ORIGIN=http://38.55.194.234,http://localhost:5173
